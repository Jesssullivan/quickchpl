Build and run ChapelCheck examples.

Execute:
```bash
# Build all examples
mason build --example

# Run specific example
mason run --example GettingStarted
mason run --example AlgebraicProperties
mason run --example CustomGenerators
```

Examples demonstrate real-world usage of ChapelCheck and serve as integration tests.
